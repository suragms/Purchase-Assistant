---
description: Pointer to root AGENTS.md (authoritative). Do not duplicate rules here.
alwaysApply: true
---

# Purchase Assistant — session pointer

Read and follow **root `AGENTS.md`** every session.

Also: `PLAN.md` (roadmap), `TASKS.md` (board), `DESIGN.md`, `DEPLOYMENT.md`, `ARCHITECTURE.md`.

Specialist rules (unique ownership only):

- `code-review.mdc` — `/codereview` workflow
- `design-quality.mdc` — Flutter token hygiene
- `figma-design-system.mdc` — Figma MCP → Flutter
- `harisree-docs.mdc` — product/docs index
